#### INIT ####
cd;git clone https://github.com/Fredmf/EHMI-OpenGL-Prak;cd EHMI-OpenGL-Prak;mkdir build;cd build;cmake ..;make;cd avis;./avis

#### UPDATE ####
cd;cd EHMI-OpenGL-Prak;git pull;cd build;cmake ..;make;cd avis;./avis

#### CLEAR ALL ####
cd;rm -r EHMI-OpenGL-Prak